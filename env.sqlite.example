# SQLite Configuration
# Copy this file to .env and update the values

# Database dialect - must be 'sqlite'
DB_DIALECT=sqlite

# SQLite database file path
# You can use either DB_FILE or DB_DATABASE
DB_FILE=./data/example.db
# OR
# DB_DATABASE=./data/example.db

# Server port
PORT=3000

# Example SQLite database setup:
# 1. Create a directory for your database files
#    mkdir -p data
#
# 2. Create a sample SQLite database
#    sqlite3 data/example.db "CREATE TABLE users (id INTEGER PRIMARY KEY, name TEXT, email TEXT);"
#    sqlite3 data/example.db "INSERT INTO users (name, email) VALUES ('John Doe', 'john@example.com');"
#    sqlite3 data/example.db "INSERT INTO users (name, email) VALUES ('Jane Smith', 'jane@example.com');"
#
# 3. Start the application
#    npm run dev
