# SQLite Configuration Example
# Copy this to .env and update with your SQLite database file path

# Database Configuration
DB_DIALECT=sqlite
DB_FILE=./path/to/your/database.db

# Server Configuration
PORT=3000

# Example SQLite database file paths:
# - Local file: ./data/my_database.db
# - Absolute path: /Users/username/databases/my_database.db
# - In-memory database: :memory:
# - Temporary database: :temp:

# Notes:
# - Make sure the SQLite database file exists and is accessible
# - The file path can be relative to the project directory or absolute
# - For in-memory databases, use :memory: as the DB_FILE value
# - For temporary databases, use :temp: as the DB_FILE value
# - SQLite databases are single-file databases, so no host/port/user/password needed
